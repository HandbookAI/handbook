{
  "name": "@handbook/resync",
  "version": "0.1.0",
  "private": true,
  "description": "Roll a handbook's derived layer forward after code changes (diff-driven incremental update)",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "@handbook/analyzer": "workspace:*",
    "@handbook/core": "workspace:*",
    "@handbook/llm": "workspace:*",
    "@handbook/pipeline": "workspace:*",
    "zod": "^4.0.0"
  }
}
